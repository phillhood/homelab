[project]
name = "homelab"
version = "0.1.0"
description = "Pinned toolchain for the homelab IaC repo"
requires-python = ">=3.12"
dependencies = [
    "ansible-core>=2.21",
    "ansible-lint>=26",
    "jmespath>=1.0",
]

[tool.uv]
package = false
