---
- name: Pi-hole Teleporter backup
  ansible.builtin.import_tasks: pihole_teleporter.yaml
  tags: [pihole]

- name: OPNsense config backup
  ansible.builtin.import_tasks: opnsense_config.yaml
  tags: [opnsense]

- name: Music library manifest backup
  ansible.builtin.import_tasks: music_manifest.yaml
  tags: [music]

- name: Music content backup
  ansible.builtin.import_tasks: music_content.yaml
  tags: [music-content]

- name: Forgejo dump backup
  ansible.builtin.import_tasks: forgejo_dump.yaml
  tags: [forgejo]

- name: Terraform state backup
  ansible.builtin.import_tasks: terraform_state.yaml
  tags: [terraform]

- name: Prune old backups
  ansible.builtin.import_tasks: rotate.yaml
  tags: [rotate]
