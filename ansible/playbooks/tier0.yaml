---
- name: Configure the upstream image cache
  hosts: registry
  gather_facts: true
  roles:
    - registry

- name: Configure the reverse proxy
  hosts: proxy
  gather_facts: true
  roles:
    - proxy

- name: Configure the git forge
  hosts: forge
  gather_facts: true
  roles:
    - postgresql
    - forgejo
