{{- if .Values.redis.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: scryer-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scryer-redis
  template:
    metadata:
      labels:
        app: scryer-redis
    spec:
      containers:
        - name: redis
          image: {{ .Values.redis.image }}
          ports:
            - containerPort: 6379
          resources:
            {{- toYaml .Values.redis.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: scryer-redis
spec:
  selector:
    app: scryer-redis
  ports:
    - port: 6379
      targetPort: 6379
{{- end }}
