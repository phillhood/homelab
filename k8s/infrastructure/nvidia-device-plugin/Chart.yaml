apiVersion: v2
name: nvidia-device-plugin
version: 1.0.0
description: NVIDIA device plugin for GPU support in Kubernetes
dependencies:
  - name: nvidia-device-plugin
    version: "0.17.*"
    repository: "https://nvidia.github.io/k8s-device-plugin"
