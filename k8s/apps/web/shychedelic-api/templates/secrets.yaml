---
# Placeholder - seal secrets before deploying:
#   kubectl create secret generic shychedelic-api-secrets \
#     --namespace=web \
#     --from-literal=db-password=<password> \
#     --from-literal=jwt-secret=<secret> \
#     --from-literal=jwt-refresh-secret=<secret> \
#     --from-literal=totp-encryption-key=<key> \
#     --from-literal=synapse-admin-token=<token> \
#     --from-literal=resend-api-key=<key> \
#     --dry-run=client -o yaml | \
#     kubeseal --controller-name=sealed-secrets-controller --controller-namespace=sealed-secrets -f -
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: shychedelic-api-secrets
  namespace: web
spec:
  encryptedData: {}
  template:
    metadata:
      name: shychedelic-api-secrets
      namespace: web
    type: Opaque
