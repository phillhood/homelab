apiVersion: apps/v1
kind: Deployment
metadata:
  name: shychedelic-web
  namespace: web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shychedelic-web
  template:
    metadata:
      labels:
        app: shychedelic-web
    spec:
      containers:
        - name: shychedelic-web
          image: {{ .Values.global.registry }}/shychedelic-web:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "32Mi"
              cpu: "10m"
            limits:
              memory: "64Mi"
              cpu: "50m"
---
apiVersion: v1
kind: Service
metadata:
  name: shychedelic-web
  namespace: web
spec:
  selector:
    app: shychedelic-web
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shychedelic-web
  namespace: web
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.middlewares: kube-system-rate-limit@kubernetescrd,kube-system-secure-headers@kubernetescrd
spec:
  tls:
    - hosts:
        - {{ .Values.global.hobbyDomain }}
        - www.{{ .Values.global.hobbyDomain }}
      secretName: shychedelic-tls
  rules:
    - host: {{ .Values.global.hobbyDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: shychedelic-web
                port:
                  number: 80
    - host: www.{{ .Values.global.hobbyDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: shychedelic-web
                port:
                  number: 80
