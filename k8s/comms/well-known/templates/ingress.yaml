---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: matrix-well-known
  namespace: communication
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`{{ .Values.global.hobbyDomain }}`) && PathPrefix(`/.well-known/matrix/`)
      kind: Rule
      priority: 100
      services:
        - name: matrix-well-known
          port: 80
  tls:
    secretName: hobby-wildcard-tls
