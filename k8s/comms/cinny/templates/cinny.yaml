---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cinny
  namespace: communication
  labels:
    app: cinny
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cinny
  template:
    metadata:
      labels:
        app: cinny
    spec:
      containers:
        - name: cinny
          image: ajbura/cinny:latest
          ports:
            - containerPort: 80
          volumeMounts:
            - name: config
              mountPath: /app/config.json
              subPath: config.json
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: config
          configMap:
            name: cinny-config
---
apiVersion: v1
kind: Service
metadata:
  name: cinny
  namespace: communication
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: cinny
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: cinny
  namespace: communication
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`cinny.{{ .Values.global.hobbyDomain }}`)
      kind: Rule
      services:
        - name: cinny
          port: 80
  tls:
    secretName: hobby-wildcard-tls
