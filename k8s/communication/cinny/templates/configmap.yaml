---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cinny-config
  namespace: communication
data:
  config.json: |
    {
      "defaultHomeserver": 0,
      "homeserverList": [
        "{{ .Values.global.hobbyDomain }}"
      ],
      "allowCustomHomeservers": true
    }
